1.) Tribonacci Series :

import java.util.Scanner;
public class Main
{
	public static void main(String[] args) {
		    Scanner sc = new Scanner(System.in);
	    int n = sc.nextInt();
	    int a = 0;
	    int b = 1;
	    int c = 1;
	    int next = 0;
	    System.out.println(a+" "+b+" "+c);
	    for(int i=4;i<n;i++)
	    
	    {
	        next = a+b+c;
	       a=b;
	       b=c;
	       c=next;
	       System.out.println(next);
	    }
	    
	}
}
	
2.) Harshad Number :


import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
	    int n = sc.nextInt();
	     
	            int sum = 0 ;
	            int temp = n;
        while (n>0)
        {
            int digit = n%10;
            sum = sum+digit;
            n = n/10;
        }
        System.out.println(sum);
        if(temp%sum == 0)
        {
            System.out.println("It is a Harshad Number");
        }
        else{
            System.out.printf("It is not a Harshad Number");
        }
        
    }
}


3.) Greatest Common Divisor :

import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
	    int n = sc.nextInt();
	    int a = 10;
	    int b = 20;
	    int gcd=0;
	    
	    for(int i=1;i<=a && i<=b;i++){
	        if(a%i==0 && b%i==0)
	        gcd = i;
	    }
	    System.out.println(gcd);
	    
	}
}

4.) Least Common Multiplier :

import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);

        System.out.print("Enter number1: ");
        int a = sc.nextInt();

        System.out.print("Enter number2: ");
        int b = sc.nextInt();

        int max = (a > b) ? a : b;

        for (int i = max; i <= a * b; i++) {
            if (i % a == 0 && i % b == 0) { 
                System.out.println("LCM = " + i);
                break;
            }
        }
	}
}




5.) Biggest and Smallest of a given number :

import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
        
        System.out.print("Enter a number: ");
        int number = sc.nextInt();
        number = Math.abs(number);

        int smallest = 9;
        int largest = 0;

        
        if (number == 0) {
            smallest = largest = 0;
        } else {
            while (number > 0) {
                int digit = number % 10;
                if (digit > largest)
                    largest = digit;
                if (digit < smallest)
                    smallest = digit;
                number /= 10;
            }
        }

        System.out.println("Smallest digit: " + smallest);
        System.out.println("Largest digit: " + largest);
    }
}

	





